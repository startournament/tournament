<html>
    <head>
        <link rel="shortcut icon" href="{{ url_for('static', filename='favicon.ico') }}">
        {% if title %}
            <title>{{ title }}</title>
        {% else %}
            <title>Need Title</title>
        {% endif %}
    </head>
    <body bgcolor=#00000 style="font-family: Arial; color: white">
        {% block content %}{% endblock %}
    </body>
</html>